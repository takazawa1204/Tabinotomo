<div class='top-bg-center'>
  <div class="container pb-5">
    <div class='top-bg-introduction mx-auto'>
      <div class='mx-auto px-3 py-4'>
        <%= form_with model: @itenerary, local:true do |f| %>
          <div class="float-right">
            <a href="/">
                <img class="itenerary-title-top pull-right" src=<%= asset_path('titleframe2.jpg') %> width="120" height="120">
            </a>
          </div>
          <div class="pt-3 p-md-5 mb-4 offset-md-5 mx-auto itenerary-title">
            <%= @itenerary.title %>
          </div>
          <div class='d-flex pt-3'>
            <h2 style='color:#000; font-size: 40px;'>持ち物リスト</h2>
        　</div>
          <hr class='divider'>
          <div>
            <%= f.fields_for :belongings do |belongings| %>
              <%= render "belonging_fields", f: belongings %>
            <% end %>
          </div>
          <div class="text-center mt-5">
            <%= link_to_add_association '追加', f, :belongings, class:"btn-lg btn-pink" %>
          </div>
          <div class='d-flex pt-3'>
            <h2 style='color:#000; font-size: 40px;'>スケジュール</h2>
        　</div>
          <hr class='divider'>
          <div>
            <%= f.fields_for :schedules do |schedules| %>
              <%= render "schedule_fields", f: schedules %>
            <% end %>
          </div>
          <div class="text-center mt-5">
            <%= link_to_add_association '追加', f, :schedules, class:"btn-lg btn-pink" %>
          </div>
          <div class='d-flex pt-3'>
            <h2 style='color:#000; font-size: 40px;'>アルバム</h2>
        　</div>
          <hr class='divider'>
          <div>
            <%= f.fields_for :albums do |albums| %>
              <%= render "album_fields", f: albums %>
            <% end %>
          </div>
          <div class="text-center mt-5">
            <%= link_to_add_association '追加', f, :albums, class:"btn-lg btn-pink" %>
          </div>
          <div class='d-flex pt-3'>
            <h2 style='color:#000; font-size: 40px;'>タグ</h2>
        　</div>
          <hr class='divider'>
          <div>
            <%= f.text_field :tag_name, value: @tag_list %><br>
            <p>※,(半角コンマ)を入力することで複数のタグをつけることができます。<br>例：夏,海,旅行</p>
          </div>
          <div class='d-flex pt-3'>
            <h2 style='color:#000; font-size: 40px;'>編集用パスワード</h2>
        　</div>
          <hr class='divider'>
          <%= f.password_field :confirmation_password %>
          <%= f.submit '更新' %>
        <% end %>
      </div>
    </div>
  </div>
</div>